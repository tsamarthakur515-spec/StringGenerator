{
  "name": "SessionBuilderBot",
  "description": "A Telegram bot to generate secure Pyrogram (v1/v2) and Telethon session strings for users and bots.",
  "logo": "https://files.catbox.moe/fvw6fs.jpg",
  "repository": "https://github.com/CertifiedCoders/StringGenerator",
  "keywords": [
    "telegram-bot",
    "pyrogram",
    "telethon",
    "session-generator",
    "docker-ready"
  ],
  "env": {
    "API_ID": {
      "description": "Get your API ID from https://my.telegram.org",
      "required": true
    },
    "API_HASH": {
      "description": "Get your API HASH from https://my.telegram.org",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "Your bot token from @BotFather",
      "required": true
    },
    "OWNER_ID": {
      "description": "Your Telegram numeric user ID (for admin commands)",
      "required": true
    },
    "MONGO_URI": {
      "description": "MongoDB connection URI (optional; enables stats/broadcast).",
      "required": false
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}