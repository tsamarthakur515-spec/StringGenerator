{
  "name": "SessionBuilderBot",
  "description": "A modern Telegram bot to generate secure Pyrogram & Telethon user/bot session strings with ease.",
  "logo": "https://files.catbox.moe/fvw6fs.jpg",
  "repository": "https://github.com/CertifiedCoders/StringGenerator",
  "keywords": [
    "telegram-bot",
    "pyrogram",
    "telethon",
    "session-generator",
    "docker-ready"
  ],
  "env": {
    "API_ID": {
      "description": "Get your API ID from https://my.telegram.org",
      "required": true
    },
    "API_HASH": {
      "description": "Get your API HASH from https://my.telegram.org",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "Bot token from @BotFather",
      "required": true
    },
    "OWNER_ID": {
      "description": "Your Telegram numeric user ID (admin only)",
      "required": true
    },
    "MONGO_URI": {
      "description": "MongoDB connection URI (used for user stats, broadcast, etc)",
      "required": false
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
